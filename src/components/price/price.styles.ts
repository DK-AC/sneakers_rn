import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  priceAmount: {
    fontFamily: 'OpenSans-Bold',
    color: '#3A63E0',
    fontSize: 18,
  },
  priceValue: {
    color: '#313b5d',
    fontSize: 23,
    fontFamily: 'OpenSans-Bold',
  },
});
